# Documentación UML — TallerPro

## 1. Diagrama de Casos de Uso

```
┌─────────────────────────────────────────────────────────────────┐
│                    Sistema TallerPro                            │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    <<include>>                           │  │
│  │  [Iniciar Sesión] ──────────────────────────────────────│  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  ADMINISTRADOR:                                                 │
│  ● Gestionar Usuarios (CRUD)                                   │
│  ● Gestionar Clientes (CRUD + activar/desactivar)              │
│  ● Gestionar Vehículos (registrar, editar, cambiar estado)     │
│  ● Crear Órdenes de Trabajo                                    │
│  ● Gestionar Cotizaciones (crear, aprobar, rechazar)           │
│  ● Gestionar Facturación (marcar pagada, PDF)                  │
│  ● Gestionar Inventario (agregar, actualizar stock)            │
│  ● Gestionar Citas (confirmar, rechazar, realizar)             │
│  ● Ver Reportes y Dashboard en tiempo real                     │
│  ● Gestionar Proveedores                                       │
│  ● Ver Ventas                                                  │
│                                                                 │
│  EMPLEADO:                                                      │
│  ● Ver Vehículos (cambiar estado, agregar historial)           │
│  ● Ver Órdenes de Trabajo                                      │
│  ● Gestionar Citas (confirmar, rechazar, realizar)             │
│  ● Ver Historial de Servicios                                  │
│  ● Ver Ventas/Facturas                                         │
│                                                                 │
│  CLIENTE:                                                       │
│  ● Registrar Vehículo                                          │
│  ● Ver Estado de su Vehículo                                   │
│  ● Agendar Cita (calendario)                                   │
│  ● Cancelar Cita                                               │
│  ● Ver Cotizaciones                                            │
│  ● Ver Dashboard personal                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. Diagrama de Clases (Modelos)

```
┌─────────────────┐       ┌─────────────────┐
│    Usuario      │       │    Vehiculo     │
├─────────────────┤       ├─────────────────┤
│ -conn: PDO      │       │ -conn: PDO      │
│ -tabla: string  │       │ -tabla: string  │
├─────────────────┤       ├─────────────────┤
│ +obtenerPorEmail│       │ +registrar()    │
│ +registrar()    │       │ +obtenerTodos() │
│ +actualizarPass │       │ +obtenerPorCli()│
│ +registrarInten │       │ +obtenerHistori │
│ +resetearInten  │       │ +agregarHistori │
│ +obtenerTodos() │       │ +cambiarEstado()│
│ +contarPorRol() │       │ +obtenerEstados │
└────────┬────────┘       │ +buscarCliente()│
         │                │ +crearCliente() │
         │ usa            └────────┬────────┘
         ▼                         │ usa
┌─────────────────┐                ▼
│    Database     │       ┌─────────────────┐
├─────────────────┤       │  OrdenServicio  │
│ -host           │       ├─────────────────┤
│ -db_name        │       │ -conn: PDO      │
│ -username       │       │ -tabla: string  │
│ -password       │       ├─────────────────┤
├─────────────────┤       │ +obtenerTodas() │
│ +conectar(): PDO│       │ +contarPorEst() │
└─────────────────┘       └─────────────────┘
```

---

## 3. Diagrama Entidad-Relación (BD)

```
roles (1) ──────< persona (N)
                    │
                    ├──< empleado (1:1)
                    │
                    └── [login/sesión]

cliente (1) ──────< vehiculo (N)
                        │
                        ├──< historial_vehiculo (N)
                        │       │
                        │       └── empleado (N:1)
                        │
                        └──< cotizacion (N)
                                │
                                ├──< detalle_servicio (N)
                                │       └── servicio (N:1)
                                │
                                ├──< detalle_repuesto (N)
                                │       └── repuesto (N:1)
                                │
                                └──< factura (1)
                                        └──< pago (N)

estado_vehiculo (1) ──< vehiculo (N)

productos (1) ──────< inventario (1:1)
productos (1) ──────< detalle_venta (N)

ventas (1) ─────────< detalle_venta (N)
cliente (1) ────────< ventas (N)
empleado (1) ───────< ventas (N)

persona (1) ────────< citas (N)
vehiculo (1) ───────< citas (N)
```

---

## 4. Diagrama de Secuencia — Login

```
Cliente          login.php        AuthController      BD (persona)
   │                │                   │                  │
   │──POST email────►│                  │                  │
   │  password       │──llamar login()──►│                  │
   │                 │                  │──obtenerPorEmail─►│
   │                 │                  │◄──usuario─────────│
   │                 │                  │                  │
   │                 │                  │──password_verify()│
   │                 │                  │                  │
   │                 │                  │──resetearIntentos►│
   │                 │                  │                  │
   │                 │                  │──$_SESSION[usuario]
   │                 │◄──redirect según rol               │
   │◄──dashboard─────│                  │                  │
```

---

## 5. Diagrama de Secuencia — Registrar Vehículo (Cliente)

```
Cliente    cliente_vehiculos.php   VehiculoController    BD
   │               │                      │               │
   │──POST placa───►│                     │               │
   │  marca,modelo  │──accion=registrar──►│               │
   │                │                     │──buscarCliente►│
   │                │                     │◄──null─────────│
   │                │                     │──crearCliente─►│
   │                │                     │◄──id_cliente───│
   │                │                     │──INSERT vehiculo►│
   │                │                     │◄──ok────────────│
   │                │◄──redirect+alert────│               │
   │◄──página cargada│                    │               │
```

---

## 6. Diagrama de Flujo — Proceso de Cita

```
CLIENTE                    SISTEMA                   ADMIN/EMPLEADO
   │                          │                            │
   │──Ver calendario──────────►│                           │
   │◄──días ocupados/libres────│                           │
   │                          │                            │
   │──Clic día disponible──────►│                          │
   │◄──Modal agendar───────────│                           │
   │                          │                            │
   │──POST fecha/servicio──────►│                          │
   │                          │──INSERT citas (Pendiente)  │
   │◄──Cita agendada (CIT-XXXXX)│                          │
   │                          │                            │
   │                          │──Notificación visual───────►│
   │                          │                            │──Ver tab Pendientes
   │                          │                            │──Confirmar/Rechazar
   │                          │◄──POST accion=confirmar────│
   │                          │──UPDATE estado=Confirmada  │
   │◄──Badge "Confirmada"──────│                           │
```

---

## 7. Diagrama de Componentes

```
┌─────────────────────────────────────────────────────┐
│                   NAVEGADOR                         │
│  Bootstrap 5.3 · FontAwesome · SweetAlert2          │
│  Chart.js · FullCalendar 6.1                        │
└──────────────────────┬──────────────────────────────┘
                       │ HTTP
┌──────────────────────▼──────────────────────────────┐
│                 APACHE (Laragon)                    │
│                                                     │
│  ┌─────────────┐  ┌──────────────┐  ┌───────────┐  │
│  │   Views     │  │ Controllers  │  │  Models   │  │
│  │  /dashboard │  │  Auth        │  │  Usuario  │  │
│  │  /layouts   │  │  Vehiculo    │  │  Vehiculo │  │
│  │  /usuarios  │  │  AdminOrden  │  │  (otros)  │  │
│  └─────────────┘  │  AdminCita   │  └─────┬─────┘  │
│                   │  Dashboard   │        │        │
│                   │  Data        │        │ PDO    │
│                   └──────────────┘        │        │
└───────────────────────────────────────────┼────────┘
                                            │
┌───────────────────────────────────────────▼────────┐
│              MySQL 8.0 (latoneria_pintura)          │
│  22 tablas · roles · persona · vehiculo · citas...  │
└─────────────────────────────────────────────────────┘
```
